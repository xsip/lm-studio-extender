/**
 * Invoke - Community Edition
 * An API for invoking AI image operations
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
import { URLRegexTokenPair } from './uRLRegexTokenPair';


/**
 * Invoke\'s global app configuration.  Typically, you won\'t need to interact with this class directly. Instead, use the `get_config` function from `invokeai.app.services.config` to get a singleton config object.  Attributes:     host: IP address to bind to. Use `0.0.0.0` to serve to your local network.     port: Port to bind to.     allow_origins: Allowed CORS origins.     allow_credentials: Allow CORS credentials.     allow_methods: Methods allowed for CORS.     allow_headers: Headers allowed for CORS.     ssl_certfile: SSL certificate file for HTTPS. See https://www.uvicorn.org/settings/#https.     ssl_keyfile: SSL key file for HTTPS. See https://www.uvicorn.org/settings/#https.     log_tokenization: Enable logging of parsed prompt tokens.     patchmatch: Enable patchmatch inpaint code.     models_dir: Path to the models directory.     convert_cache_dir: Path to the converted models cache directory (DEPRECATED, but do not delete because it is needed for migration from previous versions).     download_cache_dir: Path to the directory that contains dynamically downloaded models.     legacy_conf_dir: Path to directory of legacy checkpoint config files.     db_dir: Path to InvokeAI databases directory.     outputs_dir: Path to directory for outputs.     custom_nodes_dir: Path to directory for custom nodes.     style_presets_dir: Path to directory for style presets.     workflow_thumbnails_dir: Path to directory for workflow thumbnails.     log_handlers: Log handler. Valid options are \"console\", \"file=<path>\", \"syslog=path|address:host:port\", \"http=<url>\".     log_format: Log format. Use \"plain\" for text-only, \"color\" for colorized output, \"legacy\" for 2.3-style logging and \"syslog\" for syslog-style.<br>Valid values: `plain`, `color`, `syslog`, `legacy`     log_level: Emit logging messages at this level or higher.<br>Valid values: `debug`, `info`, `warning`, `error`, `critical`     log_sql: Log SQL queries. `log_level` must be `debug` for this to do anything. Extremely verbose.     log_level_network: Log level for network-related messages. \'info\' and \'debug\' are very verbose.<br>Valid values: `debug`, `info`, `warning`, `error`, `critical`     use_memory_db: Use in-memory database. Useful for development.     dev_reload: Automatically reload when Python sources are changed. Does not reload node definitions.     profile_graphs: Enable graph profiling using `cProfile`.     profile_prefix: An optional prefix for profile output files.     profiles_dir: Path to profiles output directory.     max_cache_ram_gb: The maximum amount of CPU RAM to use for model caching in GB. If unset, the limit will be configured based on the available RAM. In most cases, it is recommended to leave this unset.     max_cache_vram_gb: The amount of VRAM to use for model caching in GB. If unset, the limit will be configured based on the available VRAM and the device_working_mem_gb. In most cases, it is recommended to leave this unset.     log_memory_usage: If True, a memory snapshot will be captured before and after every model cache operation, and the result will be logged (at debug level). There is a time cost to capturing the memory snapshots, so it is recommended to only enable this feature if you are actively inspecting the model cache\'s behaviour.     model_cache_keep_alive_min: How long to keep models in cache after last use, in minutes. A value of 0 (the default) means models are kept in cache indefinitely. If no model generations occur within the timeout period, the model cache is cleared using the same logic as the \'Clear Model Cache\' button.     device_working_mem_gb: The amount of working memory to keep available on the compute device (in GB). Has no effect if running on CPU. If you are experiencing OOM errors, try increasing this value.     enable_partial_loading: Enable partial loading of models. This enables models to run with reduced VRAM requirements (at the cost of slower speed) by streaming the model from RAM to VRAM as its used. In some edge cases, partial loading can cause models to run more slowly if they were previously being fully loaded into VRAM.     keep_ram_copy_of_weights: Whether to keep a full RAM copy of a model\'s weights when the model is loaded in VRAM. Keeping a RAM copy increases average RAM usage, but speeds up model switching and LoRA patching (assuming there is sufficient RAM). Set this to False if RAM pressure is consistently high.     ram: DEPRECATED: This setting is no longer used. It has been replaced by `max_cache_ram_gb`, but most users will not need to use this config since automatic cache size limits should work well in most cases. This config setting will be removed once the new model cache behavior is stable.     vram: DEPRECATED: This setting is no longer used. It has been replaced by `max_cache_vram_gb`, but most users will not need to use this config since automatic cache size limits should work well in most cases. This config setting will be removed once the new model cache behavior is stable.     lazy_offload: DEPRECATED: This setting is no longer used. Lazy-offloading is enabled by default. This config setting will be removed once the new model cache behavior is stable.     pytorch_cuda_alloc_conf: Configure the Torch CUDA memory allocator. This will impact peak reserved VRAM usage and performance. Setting to \"backend:cudaMallocAsync\" works well on many systems. The optimal configuration is highly dependent on the system configuration (device type, VRAM, CUDA driver version, etc.), so must be tuned experimentally.     device: Preferred execution device. `auto` will choose the device depending on the hardware platform and the installed torch capabilities.<br>Valid values: `auto`, `cpu`, `cuda`, `mps`, `cuda:N` (where N is a device number)     precision: Floating point precision. `float16` will consume half the memory of `float32` but produce slightly lower-quality images. The `auto` setting will guess the proper precision based on your video card and operating system.<br>Valid values: `auto`, `float16`, `bfloat16`, `float32`     sequential_guidance: Whether to calculate guidance in serial instead of in parallel, lowering memory requirements.     attention_type: Attention type.<br>Valid values: `auto`, `normal`, `xformers`, `sliced`, `torch-sdp`     attention_slice_size: Slice size, valid when attention_type==\"sliced\".<br>Valid values: `auto`, `balanced`, `max`, `1`, `2`, `3`, `4`, `5`, `6`, `7`, `8`     force_tiled_decode: Whether to enable tiled VAE decode (reduces memory consumption with some performance penalty).     pil_compress_level: The compress_level setting of PIL.Image.save(), used for PNG encoding. All settings are lossless. 0 = no compression, 1 = fastest with slightly larger filesize, 9 = slowest with smallest filesize. 1 is typically the best setting.     max_queue_size: Maximum number of items in the session queue.     clear_queue_on_startup: Empties session queue on startup.     allow_nodes: List of nodes to allow. Omit to allow all.     deny_nodes: List of nodes to deny. Omit to deny none.     node_cache_size: How many cached nodes to keep in memory.     hashing_algorithm: Model hashing algorthim for model installs. \'blake3_multi\' is best for SSDs. \'blake3_single\' is best for spinning disk HDDs. \'random\' disables hashing, instead assigning a UUID to models. Useful when using a memory db to reduce model installation time, or if you don\'t care about storing stable hashes for models. Alternatively, any other hashlib algorithm is accepted, though these are not nearly as performant as blake3.<br>Valid values: `blake3_multi`, `blake3_single`, `random`, `md5`, `sha1`, `sha224`, `sha256`, `sha384`, `sha512`, `blake2b`, `blake2s`, `sha3_224`, `sha3_256`, `sha3_384`, `sha3_512`, `shake_128`, `shake_256`     remote_api_tokens: List of regular expression and token pairs used when downloading models from URLs. The download URL is tested against the regex, and if it matches, the token is provided in as a Bearer token.     scan_models_on_startup: Scan the models directory on startup, registering orphaned models. This is typically only used in conjunction with `use_memory_db` for testing purposes.     unsafe_disable_picklescan: UNSAFE. Disable the picklescan security check during model installation. Recommended only for development and testing purposes. This will allow arbitrary code execution during model installation, so should never be used in production.     allow_unknown_models: Allow installation of models that we are unable to identify. If enabled, models will be marked as `unknown` in the database, and will not have any metadata associated with them. If disabled, unknown models will be rejected during installation.     multiuser: Enable multiuser support. When disabled, the application runs in single-user mode using a default system account with administrator privileges. When enabled, requires user authentication and authorization.     strict_password_checking: Enforce strict password requirements. When True, passwords must contain uppercase, lowercase, and numbers. When False (default), any password is accepted but its strength (weak/moderate/strong) is reported to the user.
 */
export interface InvokeAIAppConfig { 
    /**
     * Schema version of the config file. This is not a user-configurable setting.
     */
    schema_version?: string;
    legacy_models_yaml_path?: string | null;
    /**
     * IP address to bind to. Use `0.0.0.0` to serve to your local network.
     */
    host?: string;
    /**
     * Port to bind to.
     */
    port?: number;
    /**
     * Allowed CORS origins.
     */
    allow_origins?: Array<string>;
    /**
     * Allow CORS credentials.
     */
    allow_credentials?: boolean;
    /**
     * Methods allowed for CORS.
     */
    allow_methods?: Array<string>;
    /**
     * Headers allowed for CORS.
     */
    allow_headers?: Array<string>;
    ssl_certfile?: string | null;
    ssl_keyfile?: string | null;
    /**
     * Enable logging of parsed prompt tokens.
     */
    log_tokenization?: boolean;
    /**
     * Enable patchmatch inpaint code.
     */
    patchmatch?: boolean;
    /**
     * Path to the models directory.
     */
    models_dir?: string;
    /**
     * Path to the converted models cache directory (DEPRECATED, but do not delete because it is needed for migration from previous versions).
     */
    convert_cache_dir?: string;
    /**
     * Path to the directory that contains dynamically downloaded models.
     */
    download_cache_dir?: string;
    /**
     * Path to directory of legacy checkpoint config files.
     */
    legacy_conf_dir?: string;
    /**
     * Path to InvokeAI databases directory.
     */
    db_dir?: string;
    /**
     * Path to directory for outputs.
     */
    outputs_dir?: string;
    /**
     * Path to directory for custom nodes.
     */
    custom_nodes_dir?: string;
    /**
     * Path to directory for style presets.
     */
    style_presets_dir?: string;
    /**
     * Path to directory for workflow thumbnails.
     */
    workflow_thumbnails_dir?: string;
    /**
     * Log handler. Valid options are \"console\", \"file=<path>\", \"syslog=path|address:host:port\", \"http=<url>\".
     */
    log_handlers?: Array<string>;
    /**
     * Log format. Use \"plain\" for text-only, \"color\" for colorized output, \"legacy\" for 2.3-style logging and \"syslog\" for syslog-style.
     */
    log_format?: InvokeAIAppConfig.LogFormatEnum;
    /**
     * Emit logging messages at this level or higher.
     */
    log_level?: InvokeAIAppConfig.LogLevelEnum;
    /**
     * Log SQL queries. `log_level` must be `debug` for this to do anything. Extremely verbose.
     */
    log_sql?: boolean;
    /**
     * Log level for network-related messages. \'info\' and \'debug\' are very verbose.
     */
    log_level_network?: InvokeAIAppConfig.LogLevelNetworkEnum;
    /**
     * Use in-memory database. Useful for development.
     */
    use_memory_db?: boolean;
    /**
     * Automatically reload when Python sources are changed. Does not reload node definitions.
     */
    dev_reload?: boolean;
    /**
     * Enable graph profiling using `cProfile`.
     */
    profile_graphs?: boolean;
    profile_prefix?: string | null;
    /**
     * Path to profiles output directory.
     */
    profiles_dir?: string;
    max_cache_ram_gb?: number | null;
    max_cache_vram_gb?: number | null;
    /**
     * If True, a memory snapshot will be captured before and after every model cache operation, and the result will be logged (at debug level). There is a time cost to capturing the memory snapshots, so it is recommended to only enable this feature if you are actively inspecting the model cache\'s behaviour.
     */
    log_memory_usage?: boolean;
    /**
     * How long to keep models in cache after last use, in minutes. A value of 0 (the default) means models are kept in cache indefinitely. If no model generations occur within the timeout period, the model cache is cleared using the same logic as the \'Clear Model Cache\' button.
     */
    model_cache_keep_alive_min?: number;
    /**
     * The amount of working memory to keep available on the compute device (in GB). Has no effect if running on CPU. If you are experiencing OOM errors, try increasing this value.
     */
    device_working_mem_gb?: number;
    /**
     * Enable partial loading of models. This enables models to run with reduced VRAM requirements (at the cost of slower speed) by streaming the model from RAM to VRAM as its used. In some edge cases, partial loading can cause models to run more slowly if they were previously being fully loaded into VRAM.
     */
    enable_partial_loading?: boolean;
    /**
     * Whether to keep a full RAM copy of a model\'s weights when the model is loaded in VRAM. Keeping a RAM copy increases average RAM usage, but speeds up model switching and LoRA patching (assuming there is sufficient RAM). Set this to False if RAM pressure is consistently high.
     */
    keep_ram_copy_of_weights?: boolean;
    ram?: number | null;
    vram?: number | null;
    /**
     * DEPRECATED: This setting is no longer used. Lazy-offloading is enabled by default. This config setting will be removed once the new model cache behavior is stable.
     */
    lazy_offload?: boolean;
    pytorch_cuda_alloc_conf?: string | null;
    /**
     * Preferred execution device. `auto` will choose the device depending on the hardware platform and the installed torch capabilities.<br>Valid values: `auto`, `cpu`, `cuda`, `mps`, `cuda:N` (where N is a device number)
     */
    device?: string;
    /**
     * Floating point precision. `float16` will consume half the memory of `float32` but produce slightly lower-quality images. The `auto` setting will guess the proper precision based on your video card and operating system.
     */
    precision?: InvokeAIAppConfig.PrecisionEnum;
    /**
     * Whether to calculate guidance in serial instead of in parallel, lowering memory requirements.
     */
    sequential_guidance?: boolean;
    /**
     * Attention type.
     */
    attention_type?: InvokeAIAppConfig.AttentionTypeEnum;
    /**
     * Slice size, valid when attention_type==\"sliced\".
     */
    attention_slice_size?: InvokeAIAppConfig.AttentionSliceSizeEnum;
    /**
     * Whether to enable tiled VAE decode (reduces memory consumption with some performance penalty).
     */
    force_tiled_decode?: boolean;
    /**
     * The compress_level setting of PIL.Image.save(), used for PNG encoding. All settings are lossless. 0 = no compression, 1 = fastest with slightly larger filesize, 9 = slowest with smallest filesize. 1 is typically the best setting.
     */
    pil_compress_level?: number;
    /**
     * Maximum number of items in the session queue.
     */
    max_queue_size?: number;
    /**
     * Empties session queue on startup.
     */
    clear_queue_on_startup?: boolean;
    allow_nodes?: Array<string> | null;
    deny_nodes?: Array<string> | null;
    /**
     * How many cached nodes to keep in memory.
     */
    node_cache_size?: number;
    /**
     * Model hashing algorthim for model installs. \'blake3_multi\' is best for SSDs. \'blake3_single\' is best for spinning disk HDDs. \'random\' disables hashing, instead assigning a UUID to models. Useful when using a memory db to reduce model installation time, or if you don\'t care about storing stable hashes for models. Alternatively, any other hashlib algorithm is accepted, though these are not nearly as performant as blake3.
     */
    hashing_algorithm?: InvokeAIAppConfig.HashingAlgorithmEnum;
    remote_api_tokens?: Array<URLRegexTokenPair> | null;
    /**
     * Scan the models directory on startup, registering orphaned models. This is typically only used in conjunction with `use_memory_db` for testing purposes.
     */
    scan_models_on_startup?: boolean;
    /**
     * UNSAFE. Disable the picklescan security check during model installation. Recommended only for development and testing purposes. This will allow arbitrary code execution during model installation, so should never be used in production.
     */
    unsafe_disable_picklescan?: boolean;
    /**
     * Allow installation of models that we are unable to identify. If enabled, models will be marked as `unknown` in the database, and will not have any metadata associated with them. If disabled, unknown models will be rejected during installation.
     */
    allow_unknown_models?: boolean;
    /**
     * Enable multiuser support. When disabled, the application runs in single-user mode using a default system account with administrator privileges. When enabled, requires user authentication and authorization.
     */
    multiuser?: boolean;
    /**
     * Enforce strict password requirements. When True, passwords must contain uppercase, lowercase, and numbers. When False (default), any password is accepted but its strength (weak/moderate/strong) is reported to the user.
     */
    strict_password_checking?: boolean;
}
export namespace InvokeAIAppConfig {
    export type LogFormatEnum = 'plain' | 'color' | 'syslog' | 'legacy';
    export const LogFormatEnum = {
        Plain: 'plain' as LogFormatEnum,
        Color: 'color' as LogFormatEnum,
        Syslog: 'syslog' as LogFormatEnum,
        Legacy: 'legacy' as LogFormatEnum
    };
    export type LogLevelEnum = 'debug' | 'info' | 'warning' | 'error' | 'critical';
    export const LogLevelEnum = {
        Debug: 'debug' as LogLevelEnum,
        Info: 'info' as LogLevelEnum,
        Warning: 'warning' as LogLevelEnum,
        Error: 'error' as LogLevelEnum,
        Critical: 'critical' as LogLevelEnum
    };
    export type LogLevelNetworkEnum = 'debug' | 'info' | 'warning' | 'error' | 'critical';
    export const LogLevelNetworkEnum = {
        Debug: 'debug' as LogLevelNetworkEnum,
        Info: 'info' as LogLevelNetworkEnum,
        Warning: 'warning' as LogLevelNetworkEnum,
        Error: 'error' as LogLevelNetworkEnum,
        Critical: 'critical' as LogLevelNetworkEnum
    };
    export type PrecisionEnum = 'auto' | 'float16' | 'bfloat16' | 'float32';
    export const PrecisionEnum = {
        Auto: 'auto' as PrecisionEnum,
        Float16: 'float16' as PrecisionEnum,
        Bfloat16: 'bfloat16' as PrecisionEnum,
        Float32: 'float32' as PrecisionEnum
    };
    export type AttentionTypeEnum = 'auto' | 'normal' | 'xformers' | 'sliced' | 'torch-sdp';
    export const AttentionTypeEnum = {
        Auto: 'auto' as AttentionTypeEnum,
        Normal: 'normal' as AttentionTypeEnum,
        Xformers: 'xformers' as AttentionTypeEnum,
        Sliced: 'sliced' as AttentionTypeEnum,
        TorchSdp: 'torch-sdp' as AttentionTypeEnum
    };
    export type AttentionSliceSizeEnum = 'auto' | 'balanced' | 'max' | '1' | '2' | '3' | '4' | '5' | '6' | '7' | '8';
    export const AttentionSliceSizeEnum = {
        Auto: 'auto' as AttentionSliceSizeEnum,
        Balanced: 'balanced' as AttentionSliceSizeEnum,
        Max: 'max' as AttentionSliceSizeEnum,
        _1: '1' as AttentionSliceSizeEnum,
        _2: '2' as AttentionSliceSizeEnum,
        _3: '3' as AttentionSliceSizeEnum,
        _4: '4' as AttentionSliceSizeEnum,
        _5: '5' as AttentionSliceSizeEnum,
        _6: '6' as AttentionSliceSizeEnum,
        _7: '7' as AttentionSliceSizeEnum,
        _8: '8' as AttentionSliceSizeEnum
    };
    export type HashingAlgorithmEnum = 'blake3_multi' | 'blake3_single' | 'random' | 'md5' | 'sha1' | 'sha224' | 'sha256' | 'sha384' | 'sha512' | 'blake2b' | 'blake2s' | 'sha3_224' | 'sha3_256' | 'sha3_384' | 'sha3_512' | 'shake_128' | 'shake_256';
    export const HashingAlgorithmEnum = {
        Blake3Multi: 'blake3_multi' as HashingAlgorithmEnum,
        Blake3Single: 'blake3_single' as HashingAlgorithmEnum,
        Random: 'random' as HashingAlgorithmEnum,
        Md5: 'md5' as HashingAlgorithmEnum,
        Sha1: 'sha1' as HashingAlgorithmEnum,
        Sha224: 'sha224' as HashingAlgorithmEnum,
        Sha256: 'sha256' as HashingAlgorithmEnum,
        Sha384: 'sha384' as HashingAlgorithmEnum,
        Sha512: 'sha512' as HashingAlgorithmEnum,
        Blake2b: 'blake2b' as HashingAlgorithmEnum,
        Blake2s: 'blake2s' as HashingAlgorithmEnum,
        Sha3224: 'sha3_224' as HashingAlgorithmEnum,
        Sha3256: 'sha3_256' as HashingAlgorithmEnum,
        Sha3384: 'sha3_384' as HashingAlgorithmEnum,
        Sha3512: 'sha3_512' as HashingAlgorithmEnum,
        Shake128: 'shake_128' as HashingAlgorithmEnum,
        Shake256: 'shake_256' as HashingAlgorithmEnum
    };
}


